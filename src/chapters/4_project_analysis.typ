= PROJECT ANALYSIS AND CONCEPTS <ch_project_analysis>

This chapter analyzes the main requirements and workflow of the project.
  The analysis is based on the assigned development tasks, client
  feedback, and the existing architecture of the project.

== Requirements Specification

TO FILL — Describe the requirements, both functional and non-functional,
  of the system.

=== Functional Requirements

TO FILL — List and describe the functional requirements of the system.

#figure(
  align(center)[
    #set text(size: 12pt)
    #set table(inset: 8pt)
    #table(
      columns: (30%, 70%),
      align: (center + horizon, left),
      table.header(
        table.cell(align: center)[*Function*],
        table.cell(align: center)[*Requirement Description*],
      ),
      [TO FILL], [TO FILL],
      [TO FILL], [TO FILL],
      [TO FILL], [TO FILL],
    )
  ],
  caption: [TO FILL — Functional requirements],
) <functional_requirements_table>


=== Non-Functional Requirements

TO FILL — Describe the quality attributes of the system such as
  security, performance, usability, maintainability, etc.

#figure(
  align(center)[
    #set text(size: 12pt)
    #set table(inset: 8pt)
    #table(
      columns: (25%, 75%),
      align: (center + horizon, left),
      table.header(
        table.cell(align: center)[*Attribute*],
        table.cell(align: center)[*Description*],
      ),
      [TO FILL], [TO FILL],
      [TO FILL], [TO FILL],
      [TO FILL], [TO FILL],
    )
  ],
  caption: [TO FILL — Non-functional requirements],
) <non_functional_requirements_table>


== Task Breakdowns and Responsibilities

TO FILL — List the tasks that were assigned during the internship, with
  descriptions and types.

#figure(
  align(center)[
    #set text(size: 12pt)
    #set table(inset: 8pt)
      #table(
      columns: (30%, 55%, 15%),
      align: (left, left, center + horizon),
      table.header(
        table.cell(align: center)[*Task*],
        table.cell(align: center)[*Description*],
        table.cell(align: center)[*Type*],
      ),
      [TO FILL], [TO FILL], [TO FILL],
      [TO FILL], [TO FILL], [TO FILL],
      [TO FILL], [TO FILL], [TO FILL],
    )
  ],
  caption: [TO FILL — Task breakdowns and responsibilities],
) <task_breakdowns_table>


== Use Case Analysis

TO FILL — Describe the main user roles and their use cases. Include use
  case diagrams for each role.

== Activity Diagram

TO FILL — Describe the overall system flow.

#figure(
  image("../media/placeholder.png", width: 90%),
  caption: [TO FILL — Add activity diagram],
) <activity_diagram>
